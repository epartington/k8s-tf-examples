apiVersion: v1
kind: Service
metadata:
  name: {{ include "airsgateway.fullname" . }}
  labels:
    {{- include "gateway.labels" . }}
    {{- with .Values.service.additionalLabels }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
  {{- with .Values.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.service.type }}
  ports:
    {{- if eq (include "gateway.enabled" .) "true" }}
    - port: {{ .Values.service.port }}
      targetPort: gateway
      protocol: TCP
      name: gateway
    {{- end }}
    {{- if eq (include "mcp.enabled" .) "true" }}
    - port: {{ include "mcp.containerPort" . }}
      targetPort: mcp
      protocol: TCP
      name: mcp
    {{- end }}
  selector:
    {{- include "airsgateway.selectorLabels" . | nindent 4 }}
---
{{- if and .Values.mcpService.enabled (eq (include "mcp.enabled" .) "true") }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "airsgateway.fullname" . }}-mcp
  labels:
    {{- include "gateway.labels" . }}
    {{- with .Values.mcpService.additionalLabels }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
  {{- with .Values.mcpService.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.mcpService.type }}
  ports:
    - port: {{ .Values.mcpService.port }}
      targetPort: mcp
      protocol: TCP
      name: mcp
  selector:
    {{- include "airsgateway.selectorLabels" . | nindent 4 }}
{{- end }}